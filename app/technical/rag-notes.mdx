# Enterprise RAG Notes

This MDX page is wired into the Next.js app so deeper technical notes can be added without changing the portfolio shell.

```ts
type RetrievalTrace = {
  query: string
  chunks: number
  citations: string[]
  latencyMs: number
}
```
